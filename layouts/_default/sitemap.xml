{{- /* Custom sitemap: excludes tags, categories and pagination pages */ -}}
{{- $pages := .Data.Pages -}}
{{- print "<?xml version=\"1.0\" encoding=\"utf-8\" standalone=\"yes\"?>" | safeHTML }}
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9"
        xmlns:xhtml="http://www.w3.org/1999/xhtml">
  {{- range $pages }}
  {{- $skip := false }}
  {{- if or (eq .Kind "taxonomy") (eq .Kind "term") }}
    {{- $skip = true }}
  {{- end }}
  {{- if .Params.sitemap_exclude }}
    {{- $skip = true }}
  {{- end }}
  {{- if not $skip }}
  <url>
    <loc>{{ .Permalink }}</loc>
    {{- if not .Lastmod.IsZero }}
    <lastmod>{{ .Lastmod.Format "2006-01-02" }}</lastmod>
    {{- end }}
    {{- with .Sitemap.ChangeFreq }}
    <changefreq>{{ . }}</changefreq>
    {{- end }}
    {{- if ge .Sitemap.Priority 0.0 }}
    <priority>{{ .Sitemap.Priority }}</priority>
    {{- end }}
    {{- range .AllTranslations }}
    <xhtml:link rel="alternate" hreflang="{{ .Language.Lang }}" href="{{ .Permalink }}"/>
    {{- end }}
    {{- if .IsTranslated }}
    <xhtml:link rel="alternate" hreflang="x-default" href="{{ (index .Translations 0).Permalink }}"/>
    {{- end }}
  </url>
  {{- end }}
  {{- end }}
</urlset>
